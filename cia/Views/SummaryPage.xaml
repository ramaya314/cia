<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewCells="clr-namespace:cia.Views.Cells"
             x:Class="cia.Views.SummaryPage">
    <ContentPage.Content>

        <Grid
                    VerticalOptions="FillAndExpand" 
                    HorizontalOptions="FillAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>


            <ListView x:Name="MainListview" 
                        HasUnevenRows="true" 
                        ItemsSource="{Binding CellModelList}"
                        ItemSelected="MainListview_ItemSelected"
                        IsPullToRefreshEnabled="false"
                        Grid.Row="0"
                        Grid.Column="0">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCells:SummaryViewCell OnBetterAlternativeButtonClicked="SummaryViewCell_OnBetterAlternativeButtonClicked" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </Grid>
    </ContentPage.Content>
</ContentPage>